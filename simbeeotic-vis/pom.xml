<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>harvard.robobees.simbeeotic</groupId>
    <artifactId>simbeeotic-vis</artifactId>
    <name>Colony Simulator Visualization Component</name>
    <description>A 3D visualization for the Colony simulator.</description>

    <parent>
        <groupId>harvard.robobees</groupId>
        <artifactId>simbeeotic</artifactId>
        <version>3.0.0-SNAPSHOT</version>
    </parent>

    <dependencies>
        <dependency>
            <groupId>harvard.robobees.simbeeotic</groupId>
            <artifactId>simbeeotic-core</artifactId>
        </dependency>
        <dependency>
            <groupId>net.java.dev.java3d</groupId>
            <artifactId>j3d-core</artifactId>
        </dependency>
        <dependency>
            <groupId>net.java.dev.java3d</groupId>
            <artifactId>j3d-utils</artifactId>
        </dependency>
    </dependencies>

    <!-- todo: remove this when we get a real repo -->
    <distributionManagement>
        <repository>
            <id>hive</id>
            <name>RoboBees Maven Repository</name>
            <!-- release from a tagged version -->
            <url>file://${basedir}/../../../../../mavenrepo</url>
        </repository>
        <snapshotRepository>
            <id>hive-snapshot</id>
            <name>RoboBees Maven Snapshot Repository</name>
            <!-- release from the trunk -->
            <url>file://${basedir}/../../../../mavenrepo-snapshots</url>
            <uniqueVersion>false</uniqueVersion>
        </snapshotRepository>
    </distributionManagement>
</project>